<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="M-PESA Interceptor"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!-- ===== LSPosed / Xposed module metadata ===== -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="Intercepts M-PESA SMS, parses txn data, posts to n8n webhook." />

        <!-- Minimum Xposed API version (82 = stable LSPosed) -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- Default scope hint (LSPosed reads this; user still confirms in Manager) -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
